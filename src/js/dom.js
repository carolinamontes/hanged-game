const checkButtonElement = document.getElementById('button');
const letterElement = document.getElementById('letter-container');
const wordHiddenElement = document.getElementById('word-display');
const attempsElement = document.getElementById('attempts');
const letterHistoryElement = document.getElementById('letter-history');
const attempsNumber = document.getElementById('numbers');
const loserElement = document.getElementById('loser');
const winnerElement = document.getElementById('winner');

export {
	checkButtonElement,
	letterElement,
	wordHiddenElement,
	attempsElement,
	letterHistoryElement,
	attempsNumber,
	loserElement,
	winnerElement
};
